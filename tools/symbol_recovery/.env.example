# LLM API Configuration

# API Key（必需）
# 从对应的 API 服务提供商获取
LLM_API_KEY=your_api_key_here

# API Base URL（可选）
# 如果不设置，将使用空字符串，由调用方决定默认值
LLM_BASE_URL=https://api.poe.com/v1

# Model Name（可选）
# 如果不设置，默认使用 GPT-5
LLM_MODEL=GPT-5
