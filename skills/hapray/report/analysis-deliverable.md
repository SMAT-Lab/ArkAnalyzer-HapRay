> 主 Skill 路由：[`SKILL.md`](../SKILL.md) 阶段 6  
> 前置：阶段 4 [`analysis/`](../analysis/README.md) high-load 线索（读 `report/`，**不依赖 update**）；阶段 5 [`root-cause/empty-frame`](../root-cause/empty-frame.md) 的空刷 `root_cause.md`（A）+ Agent 全面根因（B）。可选阶段 3 符号恢复产物（如有）。

# Agent 分析交付报告（高负载分析报告，analysis-deliverable）

与 HapRay 自动报告（`hapray_report.html` / `root_cause.md`）区分：本文档规范 **Agent 写给用户的独立高负载分析报告（Markdown）**，默认路径：

`reports/hapray-analysis-<YYYYMMDD>-<app>-load-<suffix>.md`（`<PROJECT_ROOT>/reports/` 下）

其中 `<suffix>` 为以下之一：

| 后缀 | 使用场景 | 说明 |
|------|---------|------|
| `pre` | 首次分析（修改前） | 代码优化前的基线报告 |
| `post` | 优化后复测（修改后） | 代码优化后重新采集的第二轮报告 |
| `comparison` | 修改前后对比 | 同时引用 pre 和 post 数据的对比报告 |

**命名示例：**

```
reports/hapray-analysis-20260601-saltplayer-load-pre.md
reports/hapray-analysis-20260601-saltplayer-load-post.md
reports/hapray-analysis-20260601-saltplayer-load-comparison.md
```

---

## 阶段 6 门禁（MUST，写交付报告前）

| 条件 | 动作 |
|------|------|
| 阶段4 high-load 已完成 | 报告主体（步骤指标、符号/帧/线程/IPC 热点）来自 high-load 真实产物，**非** `summary.json` 复述 |
| 存在 `trace_emptyFrame.json` 且做了空刷根因 | Read `<用例>/report/root_cause.md`，将空刷 Top Suspects **融合**进第三章（与 B 全面根因统一排序） |
| `root_cause.md` 含 `Pending Agent Inference` | **禁止**写阶段 6；先按 [`empty-frame.md`](../root-cause/empty-frame.md) §〇.3 完成 Agent 闭环 |
| Agent 全面根因（B）已对非空刷高负载问题做源码级定位 | 各条作为独立根因条目进入第三章三段式 |
| 用户 §0 跳过源码 / 无空刷 | root-cause 降级（仅证据无行号 / 仅 perf 产物级）；在「未覆盖项」说明 |

```text
阶段 4 high-load → 阶段 5 root-cause（A 空刷 + B 全面）→ 阶段 6 落盘融合
```

---

## 报告类型与产出规则

### 单轮测试（首次分析）

产出 **1 份** pre 报告：

```
reports/hapray-analysis-<YYYYMMDD>-<app>-load-pre.md
```

### 双轮测试（优化后复测）

产出 **3 份**报告：

| 报告 | 后缀 | 核心内容 |
|------|------|---------|
| 修改前报告 | `pre` | 基线数据 + 根因分析 + 优化建议 |
| 修改后报告 | `post` | 优化后数据 + 根因分析（含优化效果评估）+ 后续优化建议 |
| 修改前后对比报告 | `comparison` | 逐指标对比 + 逐项优化效果评估 + 综合结论 |

**禁止**用 1 份对比报告替代 3 份独立报告。每份报告必须能独立阅读，包含完整上下文。

---

## 报告结构与模板（MUST）

### 通用规则

1. **中文撰写**，中文章节编号（一、二、三…）
2. 根因按 **P0/P1/P2/P3** 优先级排列，每条包含 **HapRay 证据 + 源码根因 + 修复建议** 三段式
3. 源码引用格式：`文件相对路径:行号`，代码块标注语言类型
4. 优先级标签格式：`[严重] P0` / `[中等] P1` / `[低] P2` / `[建议] P3`
5. 数字使用人类可读格式：指令数用 M/B 单位（如 5.42B），百分比保留 2 位小数
6. 表格中关键数值**加粗**标记

### pre / post 报告结构（10 章）

```markdown
# <应用名>（<应用英文名>）HarmonyOS 性能测试分析报告（修改前/修改后）

**应用：** <包名> <版本号>  
**日期：** <YYYY-MM-DD>  
**工具：** HapRay <版本号>  
**设备：** <设备SN>（分辨率 <宽>×<高>）  
**源码路径：** <源码绝对路径>（HarmonyOS 原生源码，非反编译）  
**数据路径：** reports/<timestamp>/<用例名>

---

## 一、概述

基于 HapRay v<版本> 对<应用名> <N> 个核心场景进行性能测试，并结合 HarmonyOS 原生源码进行根因定位。

**整体评价：** <1-2 句总结应用功能状态 + 核心瓶颈>

**严重程度：<低/中/中高/高>** — <一句话解释>

---

## 二、测试场景与步骤指标

| 步骤 | 场景 | 总指令数 | 主线程占比 | 空刷帧数 | 离树节点占比 | 超大图片 | 超额内存(MB) | 组件复用率 | 动画帧数 |
|------|------|---------|-----------|---------|-------------|---------|-------------|-----------|---------|
| 1 | <场景名> | <指令数> | <占比> | <数>(<率>) | <率>(<离树>/<总数>) | <超>/<总> | <MB> | <率>(<构建数>) | <帧数> |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

**关键发现：**

- <发现 1：用数据说话，加粗关键数值>
- <发现 2>
- <发现 3-5>

---

## 三、根因分析与源码定位

### 3.1 [严重] P0：<根因标题>

**HapRay 证据：**
- <证据 1：空刷帧数据 / root_cause 排名 / 组件命中数>
- <证据 2>
- <证据 3>

**源码根因：**

`<相对文件路径>:<行号>`：

```typescript
// 问题代码片段，用 ← 标注问题行
```

<1-3 句解释根因>

**修复建议：**

```typescript
// 修复后的代码片段
```

---

### 3.2 [严重] P0：<根因标题>

（同上格式）

---

### 3.N [低] P3：<根因标题>

（同上格式，低优先级可简化修复建议）

---

## 四、符号级热点分析

### 4.1 libark_jsruntime.so 顶级符号（步骤 <最重步骤>）

| 函数 | 深度 | 指令数 | 占比 |
|------|------|--------|------|
| `<函数名>` | <深度> | <指令数> | <占比> |
| ... | ... | ... | ... |

**解读：** <2-3 句解释热点符号含义>

### 4.2 ArkUI 调用栈热点（步骤 <最重步骤>）

| 函数 | 命中次数 | 总耗时(ms) | 平均耗时(ms) |
|------|---------|-----------|-------------|
| `<函数名>` | <命中数> | <耗时> | <平均> |
| ... | ... | ... | ... |

**与步骤 <空闲步骤> 对比：** <对比说明>

---

## 五、冗余线程分析

全部步骤均报告 **<N> 个冗余线程、<N> 条冗余指令**。<正面信号/问题描述>

---

## 六、IPC Binder 分析（步骤 <最活跃步骤> — 最活跃）

| 调用方 | 被调方 | 事务数 | 平均延迟 | QPS |
|--------|--------|-------|---------|-----|
| <调用方> | <被调方> | <事务数> | <延迟> | <QPS> |
| ... | ... | ... | ... | ... |

<1-2 句解读 QPS 是否偏高>

---

## 七、帧统计（步骤 <最重步骤>）

| 指标 | 值 |
|------|-----|
| 总帧数 | <数> |
| 平均帧耗时 | <数>ms |
| 最大帧耗时 | <数>ms |
| 最小帧耗时 | <数>ms |

---

## 八、优先级排序与修复建议

| 优先级 | 问题 | 预期效果 | 改动量 |
|--------|------|---------|--------|
| **P0** | <问题> | <预期效果> | <小/中/大> |
| **P1** | <问题> | <预期效果> | <小/中/大> |
| **P2** | <问题> | <预期效果> | <小/中/大> |
| **P3** | <问题> | <预期效果> | <小/中/大> |

---

## 九、待修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `<相对路径>` | <修改内容描述> |
| ... | ... |

---

## 十、测试复现信息

- **测试脚本：** `<testcases路径>`
- **HTML 报告：** `<hapray_report.html路径>`
- **原始数据：** `<summary.json路径>`
- **根因报告：** `<root_cause.md路径>`
- **中文分析报告：** `<本文件路径>`（本文件）
```

### post 报告额外章节

post 报告在 pre 报告基础上增加以下内容：

1. **一、概述** 中增加「已实施优化项」表格（优先级/优化项/文件/描述）
2. **三、根因分析与源码定位** 中每条根因增加「优化效果评估」：
   - 若优化生效：标注 `✅ 完全生效` 或 `⚠️ 部分生效`
   - 若优化未生效：标注 `❌ 未生效` 并说明原因
3. **八、优先级排序** 改为「后续修复建议」（仅保留未解决/新发现问题）
4. **九、待修改文件清单** 仅列出仍需修改的文件

### comparison 报告结构（10 章）

```markdown
# <应用名>（<应用英文名>）HarmonyOS 性能测试对比报告（修改前 vs 修改后）

**应用：** <包名> <版本号>  
**日期：** <YYYY-MM-DD>  
**工具：** HapRay <版本号>  
**设备：** <设备SN>（分辨率 <宽>×<高>）  
**修改前数据：** reports/<pre_timestamp>/<用例名>  
**修改后数据：** reports/<post_timestamp>/<用例名>

---

## 一、概述

本报告对<应用名>代码优化前后的 HapRay 性能测试数据进行全面对比。优化涉及 <N> 项代码修改，覆盖<优化范围一句话>。

**优化项汇总：**

| # | 优先级 | 优化项 | 文件 | 状态 |
|---|--------|--------|------|------|
| 1 | P0 | <优化项> | <文件> | <完全生效/部分生效/未生效> |
| ... | ... | ... | ... | ... |

**结论：** <1-2 句总结优化总体效果>

---

## 二、步骤指标对比

### 2.1 总指令数

| 步骤 | 场景 | 修改前 | 修改后 | 减少 | 降幅 |
|------|------|--------|--------|------|------|
| 1 | <场景> | <前> | <后> | <差> | **<率>** |
| ... | ... | ... | ... | ... | ... |
| **合计** | | **<前>** | **<后>** | **<差>** | **<率>** |

### 2.2 主线程占比

| 步骤 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
| ... | ... | ... | ... |

### 2.3 空刷帧

| 步骤 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
| ... | ... | ... | ... |

### 2.4 组件树与图片

| 指标 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
| ... | ... | ... | ... |

---

## 三、符号级热点对比（步骤 <最重步骤>）

### 3.1 libark_jsruntime.so 顶级符号

| 符号 | 修改前指令数 | 修改前% | 修改后指令数 | 修改后% | 变化 |
|------|-------------|---------|-------------|---------|------|
| ... | ... | ... | ... | ... | ... |

**关键发现：** <对比解读>

---

## 四、ArkUI 调用栈对比

### 4.1 步骤 <N> 组件热点

| 函数 | 修改前命中 | 修改前耗时(ms) | 修改后命中 | 修改后耗时(ms) | 命中变化 | 耗时变化 |
|------|-----------|---------------|-----------|---------------|---------|---------|
| ... | ... | ... | ... | ... | ... | ... |

---

## 五、各项优化效果逐项评估

### 5.1 P0：<优化项名称>

| 指标 | 修改前 | 修改后 | 评估 |
|------|--------|--------|------|
| <指标1> | <前值> | <后值> | ✅/⚠️/❌ <说明> |
| <指标2> | <前值> | <后值> | ✅/⚠️/❌ <说明> |

**评估：<完全生效/部分生效/未生效>。** <1-2 句解释>

（每项优化一个子章节，5.1-5.N）

---

## 六、帧统计对比（步骤 <最重步骤>）

| 指标 | 修改前 | 修改后 |
|------|--------|--------|
| ... | ... | ... |

---

## 七、IPC Binder 对比（步骤 <最重步骤>）

| 调用方 | 被调方 | 修改前事务数 | 修改后事务数 | 变化 |
|--------|--------|------------|------------|------|
| ... | ... | ... | ... | ... |

---

## 八、综合结论

### 8.1 已验证的优化效果

| # | 效果 | 证据 | 置信度 |
|---|------|------|--------|
| ... | ... | ... | HIGH/MED |

### 8.2 未改善/部分改善的项

| # | 问题 | 当前状态 | 原因 |
|---|------|---------|------|
| ... | ... | ... | ... |

### 8.3 优化效果总结

| 维度 | 改善程度 | 说明 |
|------|---------|------|
| CPU 指令总量 | ⭐⭐⭐☆☆ | <说明> |
| 动画渲染效率 | ⭐⭐⭐⭐⭐ | <说明> |
| 空刷帧 | ⭐⭐⭐☆☆ | <说明> |
| 主线程过载 | ⭐☆☆☆☆ | <说明> |
| 内存效率 | ⭐☆☆☆☆ | <说明> |
| 组件复用 | ⭐☆☆☆☆ | <说明> |

---

## 九、后续优化建议

| 优先级 | 问题 | 建议 | 预期效果 |
|--------|------|------|---------|
| **P0** | <问题> | <建议> | <预期> |
| ... | ... | ... | ... |

---

## 十、测试复现信息

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 测试脚本 | <路径> | 同 |
| HTML 报告 | <路径> | <路径> |
| 原始数据 | <路径> | <路径> |
| 根因报告 | <路径> | <路径> |
| 分析报告 | <pre报告路径> | <post报告路径> |
| 对比报告 | <comparison报告路径>（本文件） | — |
```

---

## §根因内容融合规范（A 空刷 + B 全面）

**位置**：所有根因（空刷 A + 全面 B）融合到 **第三章「根因分析与源码定位」**，按 P0/P1/P2/P3 **统一排序**，不为空刷单独成章。

**内容要求（MUST）**：

1. **A 空刷**：**Read** 磁盘上的 `root_cause.md`（禁止凭记忆缩写），将 Top Suspects 按置信度映射：HIGH→P0、MED→P1、LOW→P2/P3；每条「HapRay 证据」引用其具体数据（空刷帧数、嫌疑排名、触发链路）；Caveats → 融入「未覆盖项」。
2. **B 全面**：阶段4 high-load 挖出的非空刷高负载问题（SO/符号热点、高负载帧、冗余线程、IPC、内存、组件复用），每条由 Agent 借源码定位后作为**独立根因条目**进入第三章三段式，与 A 一起按优先级排序。
3. A 与 B **去重合并**：若某空刷嫌疑与某高负载热点指向同一源码点，合并为一条，证据取两侧并集。

**禁止**：

- 仅写 5 行摘要表 + 外链
- 阶段 5 未完成却声称根因已分析
- 融合内容与 `root_cause.md` 不一致（若需补充 Agent 解读，写在结论分级，不覆盖根因正文）
- 将 root_cause.md 全文机械复制为独立章节（必须融合到 §三 的三段式结构中）
- 只写空刷（A）而遗漏阶段4挖出的其余高负载根因（B）

---

## 会话变量表（每阶段更新）

| 变量 | 说明 |
|------|------|
| `workspace_layout_done` | 已执行 `ensure-workspace-layout.sh`（macOS 报告重定向） |
| `path_prompt_done` | §0 路径门禁完成 |
| `skill_read_done` | 已 Read 主 SKILL + 当前阶段文档 |
| `path_prompt_state` | `waiting_source` / `waiting_so` / `waiting_confirm` / `done` |
| `app_packages_dir_user` | 空刷根因输入 → `--app-packages-dir`（见 [`root-cause/empty-frame.md`](../root-cause/empty-frame.md)） |
| `so_dir_user` | 符号恢复 SO → `--so_dir` |
| `collection_mode` | `predefined` / `agent-authored` / `gui-agent` |
| `ui_mapping_mode` | `id` / `text` / `coordinate-only` |
| `prepare_passed` | `true` / `false` |
| `root_cause_ready` | `true`：正式 `root_cause.md` 且无 Pending；阶段 6 前置条件 |
| `report_suffix` | `pre` / `post` / `comparison`；决定报告类型与结构 |

---

## 合并来源

| 阶段 | 写入交付报告的内容 |
|------|-------------------|
| 3（可选）符号恢复 | 增强火焰图符号级热点 → §四（未做则标注「建议符号恢复」） |
| 4 analysis high-load | `report/` 真实产物（`summary.json` 步骤指标、`more_flame_graph.json`/`perf.db` 符号-SO 热点、`trace_*.json` 帧/IPC/复用、`redundant_thread_analysis.json`、`memory_report.xlsx`）→ 步骤指标 §二、热点 §四、新发现融入 §三 |
| 5 root-cause（A 空刷） | `root_cause.md` 的 Top Suspects → 融入 §三 HapRay 证据段；Caveats → 未覆盖项 |
| 5 root-cause（B 全面） | 非空刷高负载问题的源码级根因 → §三 独立条目，与 A 统一排序 |

**禁止**：无证据的伪分析；仅复述 HTML 而无阶段4挖掘；有空刷却未引用 `root_cause.md` 数据却声称完成根因分析；只写空刷而遗漏阶段4其余高负载根因（B）。

---

## 验收清单（落盘前自检）

- [ ] 报告文件名符合 `<app>-load-<suffix>` 命名规范
- [ ] pre/post 报告包含完整 10 章结构（概述→步骤指标→根因分析→符号热点→冗余线程→IPC→帧统计→优先级排序→待修改文件→测试复现）
- [ ] comparison 报告包含完整 10 章结构（概述→步骤指标对比→符号热点对比→调用栈对比→逐项评估→帧统计对比→IPC对比→综合结论→后续建议→测试复现）
- [ ] §三 每条根因包含三段式：HapRay 证据 + 源码根因 + 修复建议
- [ ] §三 根因数据已融合 root_cause.md 的 Top Suspects 结论（非机械复制全文）
- [ ] 有空刷时：根因条目引用了 root_cause.md 的具体嫌疑数据和排名
- [ ] 交付报告中 **无**「Pending Agent Inference」
- [ ] 优化建议与根因分析一致（comparison 报告中逐项评估与 §三 根因对应）
- [ ] 双轮测试产出 3 份独立报告（pre / post / comparison），每份可独立阅读
- [ ] post 报告的 §三 每条根因包含优化效果评估（✅/⚠️/❌）
- [ ] comparison 报告的 §五 逐项评估表与 pre 报告 §八 优先级排序一一对应  

---
