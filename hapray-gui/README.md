# HapRay GUI

HapRay GUI是一个集成多个工具的统一图形界面平台，整合了以下工具：

- **动态测试** (perf_testing) - 性能测试和trace收集
- **优化检测** (optimization_detector) - 二进制文件优化级别和LTO检测
- **符号恢复** (symbol_recovery) - 二进制符号恢复工具
- **静态分析** (sa) - HAP静态分析

## 功能特性

- 🎯 统一的图形界面，方便使用
- 🔧 可视化参数配置
- 📊 实时执行输出显示
- 📁 结果历史查看
- ⚙️ 工具路径配置管理

## 安装

### 方式一：使用 npm（推荐）

1. 安装 Node.js（如果还没有）

2. 运行安装脚本（会自动创建虚拟环境并安装依赖）：
```bash
npm install
```

或者手动运行：
```bash
npm run setup
```

### 方式二：手动安装

1. 创建虚拟环境：
```bash
python -m venv .venv
```

2. 激活虚拟环境：
   - Windows: `.venv\Scripts\activate`
   - Linux/Mac: `source .venv/bin/activate`

3. 安装依赖：
```bash
pip install -r requirements.txt
```

## 使用方法

### 开发模式运行

使用虚拟环境运行：
```bash
npm run run:venv
```

或直接运行：
```bash
python main.py
```

### 打包发布

打包为目录形式（推荐，启动更快）：
```bash
npm run build
```

打包为单文件（体积更大，但便于分发）：
```bash
npm run build:onefile
```

打包后的文件在 `dist/hapray-gui/` 目录下。

### 清理

清理构建文件和虚拟环境：
```bash
npm run clean
```

## 配置

首次运行会在用户目录下创建 `~/.hapray-gui/config.json` 配置文件。

可以通过"工具 -> 设置"菜单配置各个工具的路径。

## 开发

### 项目结构

```
hapray-gui/
├── main.py                    # 主程序入口
├── main.spec                  # PyInstaller 打包配置
├── setup_env.py              # 虚拟环境设置脚本
├── package.json              # npm 脚本配置
├── requirements.txt          # Python 依赖
├── core/                     # 核心公共模块
├── gui/                      # GUI相关模块
├── tools/                    # 具体工具实现
└── resources/                # 资源文件
```

### 构建说明

- `npm run setup`: 创建虚拟环境并安装依赖
- `npm run build`: 使用 PyInstaller 打包为目录形式
- `npm run build:onefile`: 打包为单文件
- `npm run clean`: 清理构建文件和虚拟环境

打包后的可执行文件位于 `dist/hapray-gui/` 目录。

## 目录结构

```
hapray-gui/
├── main.py                    # 主程序入口
├── core/                      # 核心公共模块
│   ├── base_tool.py          # 工具基类
│   ├── config_manager.py     # 配置管理器
│   ├── tool_executor.py      # 工具执行器
│   ├── result_processor.py   # 结果处理器
│   ├── file_utils.py         # 文件工具类
│   └── logger.py             # 日志管理器
├── gui/                       # GUI相关模块
│   ├── main_window.py        # 主窗口
│   ├── tool_pages.py         # 工具页面
│   ├── result_viewer.py      # 结果查看器
│   └── settings_dialog.py    # 设置对话框
├── tools/                     # 具体工具实现
│   ├── perf_testing/         # 动态测试工具
│   ├── optimization_detector/ # 优化检测工具
│   ├── symbol_recovery/      # 符号恢复工具
│   └── sa/                   # 静态分析工具
└── resources/                 # 资源文件
```

## 开发

项目使用PySide6构建GUI界面，采用模块化设计，易于扩展新工具。

## 许可证

Apache License 2.0

